\paragraph{Judge Validation Snapshot.} A lightweight regex-derived proxy and \texttt{Llama-3-70B} agreed on $95.0\%$ of $n{=}200$ held-out completions ($\kappa{=}0.724$, macro-F1${=}0.861$); the ``harmful'' bucket showed the largest mismatch ($7.5\%$). Because the proxy is hand-crafted, treat these figures as instrumentation checks rather than human inter-rater reliability.
